services:
  lettabot:
    build: .
    container_name: lettabot
    ports:
      - "9090:9090"
    environment:
      - LETTABOT_CONFIG_YAML=${LETTABOT_CONFIG_YAML}
      - WORKING_DIR=/app
      - DATA_DIR=/data
      - LOG_FORMAT=json
    volumes:
      - lettabot-data:/root/.letta
      - lettabot-credentials:/root/.lettabot/credentials
      - lettabot-research:/root/.local/share/lettabot
      - lettabot-state:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9090/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  lettabot-data:
  lettabot-credentials:
  lettabot-research:
  lettabot-state:
