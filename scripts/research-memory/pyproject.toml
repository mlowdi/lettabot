[project]
name = "research-memory"
version = "0.1.0"
description = "Persistent research database — ingest URLs, search later"
requires-python = ">=3.10,<3.14"
dependencies = [
    "click>=8.0",
    "httpx>=0.27",
    "html-to-markdown>=2.0",
]

[project.scripts]
research-memory = "main:cli"
