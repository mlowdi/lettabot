[project]
name = "web-fetch"
version = "0.1.0"
description = "Fetch a URL and convert to clean markdown"
requires-python = ">=3.10,<3.14"
dependencies = [
    "html-to-markdown>=2.0",
    "httpx>=0.27",
]
